{
  "buildCommand": "pnpm --filter docs build",
  "outputDirectory": "apps/docs/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- apps/docs packages/jarvis-ui"
}
